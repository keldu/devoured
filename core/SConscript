#!/usr/bin/env python3

Import('env')

env.core_sources = []

hdr_list = ['process_stream.h', 'unix_socket.h','devoured.h','signal_handler.h']
src_list = ['process_stream.cpp', 'unix_socket.cpp', 'devoured.cpp']

env.add_source_files(env.core_sources, src_list)

#lib = env.add_library("core", env.core_sources)
#env.Prepend(LIBS=[lib])